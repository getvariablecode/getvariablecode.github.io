let descr = document.getElementById('descr');

function Rep(){
    descr.innerHTML = `<p class="py-1 text-md Alert my-2 bg-red-100 pl-2">Note that the module functions description is not provided but its still in development and perphaps a similar better module could be provided as not part of the language standard libraries but it can have ways to be used provided by the creator.</p>`
}

Rep();
